import 'package:flutter/material.dart';

class StatForm extends StatelessWidget {
  const StatForm({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Stats Here');
  }
}
